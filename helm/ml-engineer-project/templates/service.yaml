apiVersion: v1
kind: Service
metadata:
  name: {{ template "ml-engineer-project.full_name" . }}
  labels:
    {{- include "ml-engineer-project.release_labels" . | indent 4 }}
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: {{ template "ml-engineer-project.full_name" . }}
